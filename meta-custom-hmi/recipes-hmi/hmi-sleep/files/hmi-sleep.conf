# MarknStamp HMI Display Sleep Configuration
#
# SLEEP_TIMEOUT_SEC: Inactivity duration in seconds before the display blanks
# and turns off the backlight.
# Any touch on the screen immediately wakes the display up.
# Set to 0 to keep the display always on (disable sleep mode).

SLEEP_TIMEOUT_SEC=120
