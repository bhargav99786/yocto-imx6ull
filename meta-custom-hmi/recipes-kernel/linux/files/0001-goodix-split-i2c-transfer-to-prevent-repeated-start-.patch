From 8e5f49d332612a4c16a81bf6d1396b27d422ad4e Mon Sep 17 00:00:00 2001
From: Bhargav <bhargav@custom-hmi.local>
Date: Mon, 7 Sep 2026 11:15:00 +0530
Subject: [PATCH] goodix: split i2c transfer to prevent repeated start collision and skip fallback wait

---
diff --git a/drivers/input/touchscreen/goodix.c b/drivers/input/touchscreen/goodix.c
index 25e575183dd1..910212677f46 100644
--- a/drivers/input/touchscreen/goodix.c
+++ b/drivers/input/touchscreen/goodix.c
@@ -170,7 +170,7 @@ int goodix_i2c_read(struct i2c_client *client, u16 reg, u8 *buf, int len)
 {
 	struct i2c_msg msgs[2];
 	__be16 wbuf = cpu_to_be16(reg);
-	int ret;
+	int ret, retries = 5;
 
 	msgs[0].flags = 0;
 	msgs[0].addr  = client->addr;
@@ -182,13 +182,20 @@ int goodix_i2c_read(struct i2c_client *client, u16 reg, u8 *buf, int len)
 	msgs[1].len   = len;
 	msgs[1].buf   = buf;
 
-	ret = i2c_transfer(client->adapter, msgs, 2);
-	if (ret >= 0)
-		ret = (ret == ARRAY_SIZE(msgs) ? 0 : -EIO);
+	while (retries--) {
+		ret = i2c_transfer(client->adapter, &msgs[0], 1);
+		if (ret == 1) {
+			usleep_range(150, 300);
+			ret = i2c_transfer(client->adapter, &msgs[1], 1);
+		}
+		if (ret == 1)
+			return 0;
+		usleep_range(300, 600);
+	}
 
-	if (ret)
-		dev_err(&client->dev, "Error reading %d bytes from 0x%04x: %d\n",
-			len, reg, ret);
+	ret = -EIO;
+	dev_err_ratelimited(&client->dev, "Error reading %d bytes from 0x%04x: %d\n",
+			    len, reg, ret);
 	return ret;
 }
 
@@ -204,7 +211,7 @@ int goodix_i2c_write(struct i2c_client *client, u16 reg, const u8 *buf, int len)
 {
 	u8 *addr_buf;
 	struct i2c_msg msg;
-	int ret;
+	int ret, retries = 5;
 
 	addr_buf = kmalloc(len + 2, GFP_KERNEL);
 	if (!addr_buf)
@@ -219,15 +226,19 @@ int goodix_i2c_write(struct i2c_client *client, u16 reg, const u8 *buf, int len)
 	msg.buf = addr_buf;
 	msg.len = len + 2;
 
-	ret = i2c_transfer(client->adapter, &msg, 1);
-	if (ret >= 0)
-		ret = (ret == 1 ? 0 : -EIO);
+	while (retries--) {
+		ret = i2c_transfer(client->adapter, &msg, 1);
+		if (ret == 1) {
+			kfree(addr_buf);
+			return 0;
+		}
+		usleep_range(300, 600);
+	}
 
 	kfree(addr_buf);
-
-	if (ret)
-		dev_err(&client->dev, "Error writing %d bytes to 0x%04x: %d\n",
-			len, reg, ret);
+	ret = -EIO;
+	dev_err_ratelimited(&client->dev, "Error writing %d bytes to 0x%04x: %d\n",
+			    len, reg, ret);
 	return ret;
 }
 
@@ -506,9 +517,13 @@ static void goodix_process_events(struct goodix_ts_data *ts)
 static irqreturn_t goodix_ts_irq_handler(int irq, void *dev_id)
 {
 	struct goodix_ts_data *ts = dev_id;
+	int retry = 3;
 
 	goodix_process_events(ts);
-	goodix_i2c_write_u8(ts->client, GOODIX_READ_COOR_ADDR, 0);
+
+	/* Ensure 0x814E is cleared so GT911 releases INT pin */
+	while (retry-- && goodix_i2c_write_u8(ts->client, GOODIX_READ_COOR_ADDR, 0) < 0)
+		usleep_range(500, 1000);
 
 	return IRQ_HANDLED;
 }
@@ -1382,7 +1397,7 @@ static int goodix_ts_probe(struct i2c_client *client,
 			snprintf(ts->cfg_name, sizeof(ts->cfg_name),
 				 "goodix_%s_cfg.bin", ts->id);
 
-		error = request_firmware_nowait(THIS_MODULE, true, ts->cfg_name,
+		error = request_firmware_nowait(THIS_MODULE, false, ts->cfg_name,
 						&client->dev, GFP_KERNEL, ts,
 						goodix_config_cb);
 		if (error) {
